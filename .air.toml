root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/server ./cmd/server"
bin = "tmp/server"
include_ext = ["go", "yaml", "yml", "json", "toml"]
exclude_dir = ["tmp", "vendor", ".git"]

[log]
time = true
